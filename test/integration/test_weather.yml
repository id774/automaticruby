global:
  timezone: Asia/Tokyo
  cache:
    base: /tmp
  log:
    level: info

plugins:
  - module: SubscriptionWeather
    config:
      zipcode: 166-0003
      day:     'tomorrow'

  - module: StorePermalink
    config:
      db: test_weather.db

  #- module: PublishConsole
