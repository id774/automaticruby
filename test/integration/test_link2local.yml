global:
  timezone: Asia/Tokyo
  cache:
    base: /tmp
  log:
    level: info

plugins:
  - module: SubscriptionLink
    config:
      urls:
        - http://reblog.id774.net

  - module: FilterImage

  - module: StorePermalink
    config:
      db: test_link.db

  #- module: PublishConsole

