global:
  timezone: Asia/Tokyo
  cache:
    base: /tmp
  log:
    level: info

plugins:
  - module: CustomFeedSVNLog
    config:
      target: http://svn.apache.org/repos/asf/
      fetch_items: 2

  #- module: PublishConsole

