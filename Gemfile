source :rubygems

gem 'builder' # Explicit require before simple_xlsx_writer
gem 'simple_xlsx_writer', :require => 'simple_xlsx'

gem 'sqlite3-ruby'
gem 'activesupport'
gem 'hashie'

gem 'activerecord'
gem 'gcalapi'

group :test do
  gem 'rspec'
end
